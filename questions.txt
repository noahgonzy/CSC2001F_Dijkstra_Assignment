do we need a separate run and generate function in the makefile or should it generate
and run by default.

should my 'clean' function delete all graphs

is my data vaguely accurate

should I be including more sparse graps